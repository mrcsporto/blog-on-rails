
<div class="shadow p-5 bg-light rounded">
  <div class="text-center mb-5">
    <h1><%= @post.title %></h1>
    <p class="text-muted">
      <strong><%= @post.author %></strong><br>
      <em><%= l(@post.created_at, format: :long) %></em>
    </p>
  </div>

  <p class="container col-lg-10 fs-4">
    <%= @post.body.gsub("\n", "<br>").html_safe %>
  </p>

  <div class="m-5">
    <div class="text-center">
      <h4>
        <%= pluralize(@post.comments.count, 'Comentário') %>
      </h4>
         <% if @post.comments.none? %>
          <em class="font-italic text-lowercase font-weight-light text-success"> 
            Seja o primeiro a comentar!
          </em>
        <% end %>
    </div>
  </div>

    <% @comments.each do |comment| %>
      <div class="container col-lg-8 my-3 p-4 shadow-sm border border-muted rounded">

        <div class="row d-flex flex-row">
          <div class="col fs-5">
           <strong><%= comment.author %></strong> 
          </div>
          
          <div class="col text-end">
            <em data-bs-toggle="tooltip" data-bs-placement="top" title="teste">
              há <%= time_ago_in_words comment.created_at %>
            </em> 
          </div>
        </div>

        <div class="mt-3">
          <%= comment.body %>
        </div>
      </div>
  <% end %>

  <div class="container col-lg-8 my-3 pt-4 px-4 shadow-sm border border-muted rounded">
    <%= form_with model: @comment, local: true do |form| %>
      <%= form.hidden_field :post_id, value: @post.id %>
        <div class="form-group">
          <%= form.label :author, class: 'fs-5' %>
          <%= form.text_field :author %>
        </div>

        <div class="form-group mt-2">
          <%= form.label :body, class: 'fs-5' %>
          <%= form.text_area :body %>
        </div>

        <div class="actions my-3">
          <%= form.submit 'Enviar', 
            data: { disable_with: 'Enviando...' } %>
        </div>
      <% end %>
  </div>


  
</div>

