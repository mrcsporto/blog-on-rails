<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Roboto:300,300italic,700,700italic">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/normalize/8.0.1/normalize.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/milligram/1.4.1/milligram.css">
    <link href="//netdna.bootstrapcdn.com/font-awesome/3.2.1/css/font-awesome.css" rel="stylesheet">
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all' %>
    <%= javascript_pack_tag 'application' %>
  </head>

  <body class="d-flex flex-column min-vh-100">
    <div class="bg-dark text-center sticky-top text-white mb-5 p-4">
      <%= link_to root_path, class: 'text-white' do %>
        <h1 class="display-4 fw-bold">BLOG on Rails</h1>
        <p class="lead"> 
          <em>
            Um blog compartilhado onde qualquer um pode publicar seus artigos...
          </em>
        </p>
      <% end %>
        <div class="float-right fs-3 text-muted">
          <% if user_signed_in? %>
            Logado como <strong><%= current_user.email %></strong> 
            
            <%= link_to '', edit_user_registration_path, class: 'text-white icon-user' %> |
            <%= link_to "Logout", destroy_user_session_path, method: :delete, class: 'text-white'%>
          <% else %>
            <%= link_to "Sign up", new_user_registration_path, class: 'text-white' %> |
            <%= link_to "Login", new_user_session_path, class: 'text-white' %>
          <% end %>
      </div>
    </div>


    <div class="container col-md-10 col-lg-8">
      <% flash.each do |type, message| %>
        <div class="alert alert-<%= type == 'alert' ? 'danger' : 'success' %> alert-dismissible fade show" role="alert">
          <%= message %>
        </div>
      <% end %>

      <%= yield %>
    </div>

    <footer class="bg-dark text-center sticky-bottom mt-auto text-white mt-5 p-4">
      Copyright © <a href="https://www.marcosportodev.com/" class="text-white">Marcos Porto </a> <%= Date.current.year %>
    </footer>

  </body>
</html>
